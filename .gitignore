node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode

# raw city media — optimized via WS7 pipeline in Phase 2; not committed until then
cities/*/images/
.netlify/
tsconfig.tsbuildinfo
parity-report/

# landing videos (15MB) — CLI-deployed, optimized in WS7
public/*.mp4
public/videos/
src/generated/
